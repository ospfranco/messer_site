{
  "license": "MIT",
  "scripts": {
    "prepare": "husky install",
    "dev": "bundle exec jekyll serve --livereload --drafts --future --incremental --port 3000 '$@'",
    "build": "JEKYLL_ENV=production NODE_ENV=production bundle exec jekyll build -d docs && git add ."
  },
  "dependencies": {
    "autoprefixer": "^10.3.6",
    "cssnano": "^5.0.8",
    "husky": "^5.0.9",
    "postcss": "^8.3.8",
    "postcss-cli": "8.3.1",
    "postcss-import": "^14.0.2",
    "tailwind-scrollbar-hide": "^1.0.3",
    "tailwindcss": "^2.2.16"
  }
}
